version: '3'

services:
  
  queue:
    image: redis:alpine
    command: redis-server
    ports:
     - 6379:6379
    volumes:
      - ./docker/cfg/redis/redis.conf:/usr/local/etc/redis/redis.conf

  workers:
    depends_on:
      - queue
    build:
      context: .
      dockerfile: ./docker/Dockerfile.worker
    environment:
      - CELERY_BROKER_URL=redis://queue:6379/0
      - CELERY_RESULT_BACKEND=redis://queue:6379/1
      - VCC_WORKING_DIR=/data
      - JM19_0_ENCODER=/home/deps/JM/bin/lencod_static
      - JM19_0_DECODER=/home/deps/JM/bin/ldecod_static
    command: [ "/bin/sh", "-c",
                "pip install debugpy -t /tmp && python3 /tmp/debugpy --listen 0.0.0.0:6900 -m celery -A tasks worker --loglevel=info --concurrency=4 -n worker@%h"]
    ports:
     - 6900:6900
    volumes:
      - /mnt/c/Users/nlsdv/5G-video:/data
        
  status:
    build:
      context: .
      dockerfile: ./docker/Dockerfile.flower
    depends_on:
      - queue
      - workers
    environment:
      - CELERY_BROKER_URL=redis://queue:6379/0
      - FLOWER_PORT=8888
      - CELERY_SEND_EVENTS=1
    ports:
      - 8888:8888
    volumes:
      - ./docker/cfg/redis/redis.conf:/usr/local/etc/redis/redis.conf
  

